/*
#include <stdio.h>
#include <boilerplate.h>
#include "../test_helpers.h"
#include <stdio.h>

void test_boilerplate(int* total_pass, int* total_fail)
{
    int pass = 0, fail = 0;
    printf("Testing boilerplate\n");

    printf("\nTotal Tests: %d\n",pass+fail);
    *total_pass += pass;
    *total_fail += fail;

    return 0;
}
    */
